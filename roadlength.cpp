#include <iostream>
#include "Matrix.h"

using namespace std;

int minimumLength( Matrix temp, int aNode, int bNode ) {
	if (aNode = bNode)
		return 0;// The length of same node is zero

	//Add an new matrix to store distance
	Matrix distance( temp.rows(), temp.cols() );

	for (int i = 0; i < count; ++i)
	{
		/* code */
	}

} 